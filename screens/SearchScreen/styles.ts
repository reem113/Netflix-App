import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    fontWeight: "bold",
    marginLeft: 4,
  },
  listContainer: {
    marginVertical: 16,
  },
});

export default styles;
